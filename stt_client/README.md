### Make sure you've already added Web login credentials in .env file - and the backend is running.

# Install Dependencies

```shell
pip install -r requirements.txt
```

# Run the application

```shell
streamlit run main.py
```